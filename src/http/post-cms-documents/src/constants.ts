/**
 *
 */
export enum SANITY_EVENTS {
    CREATED = 'created',
    DELETED = 'deleted',
    UPDATED = 'updated',
    ALL = 'all',
}

/**
 *
 */
export enum SANITY_EVENT_HOOKS {
    ON_CREATE = 'onCreate',
    ON_DELETE = 'onDelete',
    ON_UPDATE = 'onUpdate',
}
